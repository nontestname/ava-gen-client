<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.labcitrus.avagenclient">

    <!-- Required permission for SpeechRecognizer -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Internet permission (optional, needed for online STT) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:debuggable="true"
        android:theme="@style/Theme.Test2vaServiceStandAlone"
        tools:targetApi="31"
        tools:ignore="HardcodedDebugMode">


        <!--    test2va AndroidManifest code start    -->
        <service android:name="org.labcitrus.avagenclient.accessibility.AVAGenService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/avagen_accessibility_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/test2va_accessibility_service_config" />
            <!-- Allow microphone usage inside AccessibilityService -->
            <meta-data
                android:name="android.speech.SpeechRecognizer"
                android:value="true"/>
        </service>
        <!--    test2va AndroidManifest code end    -->

    </application>
</manifest>